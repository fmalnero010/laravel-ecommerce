{
    "commit-msg": {
        "enabled": true,
        "actions": [
            {
                "action": "\\CaptainHook\\App\\Hook\\Message\\Action\\Regex",
                "options": {
                    "regex": "/^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\\([a-z0-9-]+\\))?!?: .{1,80}/",
                    "error": "Commit message must follow Conventional Commits format:\n<type>(<scope>): <subject>\n\nTypes: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert\nExample: feat(api): add user endpoint"
                }
            }
        ]
    },
    "pre-commit": {
        "enabled": true,
        "actions": [
            {
                "action": "\\CaptainHook\\App\\Hook\\PHP\\Action\\Linting"
            },
            {
                "action": "docker compose exec -T laravel composer check:all"
            }
        ]
    },
    "post-merge": {
        "enabled": true,
        "actions": [
            {
                "action": "docker compose exec -T laravel php artisan scribe:generate"
            }
        ]
    }
}
